import 'package:flutter/material.dart';

/// Non-web stub. The GIS button is only available on Flutter web.
Widget? gisWebSignInButton({
  VoidCallback? onPressed,
  bool busy = false,
}) =>
    null;
