services:
  postgres:
    image: postgres:16-alpine
    container_name: ikayi-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ikayi
      POSTGRES_PASSWORD: ikayi
      POSTGRES_DB: ikayi_mart
    ports:
      - "5432:5432"
    volumes:
      - ikayi_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ikayi -d ikayi_mart"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: ikayi-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  ikayi_pg_data:
